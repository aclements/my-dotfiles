# pylint configuration file

#
# Modify identifier regex's to use Java-like style rules
# Add a few more bad variable names
#

[BASIC]
# Regular expression which should only match correct class names
#class-rgx=[A-Z][a-zA-Z0-9]+$
class-rgx=_?[A-Z][a-zA-Z0-9]+$

# Regular expression which should only match correct function names
#function-rgx=[a-z_][a-z0-9_]*$
function-rgx=_?[a-z][a-zA-Z0-9]*$

# Regular expression which should only match correct method names
#method-rgx=[a-z_][a-z0-9_]*$
method-rgx=(_{0,2}[a-z][a-zA-Z0-9]*|__[a-z]+__)$

# Regular expression which should only match correct argument names
#argument-rgx=[a-z_][a-z0-9_]*$
argument-rgx=[a-z][a-zA-Z0-9]*_?$

# Regular expression which should only match correct variable names
#variable-rgx=[a-z_][a-z0-9_]*$
variable-rgx=[a-z][a-zA-Z0-9]*_?$

# Good variable names which should always be accepted, separated by a comma
#good-names=i,j,k,ex,Run
good-names=i,j,k,x,ex,Run

# Bad variable names which should always be refused, separated by a comma
#bad-names=foo,bar,baz,toto,tutu,tata
bad-names=foo,bar,baz,foobar,xyzzy,toto,tutu,tata

# List of builtins function names that should not be used, separated by a comma
bad-functions=map,filter,apply,input
